events { }
http {
	server {
		listen 443	ssl;
		listen [::]:443	ssl;
		server_name	bguyot.42.fr;
		
		ssl_certificate		/etc/ssl/nginx.crt;
		ssl_certificate_key	/etc/ssl/nginx.key;
		ssl_protocols 		TLSv1.3;

		location / {
			proxy_pass http://wordpress:9000;
		}
	}
}